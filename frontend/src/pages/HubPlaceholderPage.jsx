export default function HubPlaceholderPage({ title }) {
  return (
    <section className="hub-placeholder-page">
      <div className="hub-placeholder-content">
        <h1>{title}</h1>
      </div>
    </section>
  );
}
